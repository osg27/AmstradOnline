// Runtime shapes are documented here because the main application is JavaScript.
// Local media always retains its browser File object; createManifest strips it.
export const LOCAL_GAME_SOURCE = 'local-amiga-library';

